"use strict";
let greet;
const logDetails = (uid, item) => {
    console.log(`${item} has a uid of ${uid}`);
};
greet = (user) => console.log(JSON.stringify(user));
// function signature
// ex 1
let greet1;
